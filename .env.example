# Reference only: export the variables you need before running the installer.
# This project does not automatically load .env files.
CURSOR_MACOS_SIGN_IDENTITY=
CURSOR_GPT_LINK_HOME=
CHATGPT_TEST_MODEL=
